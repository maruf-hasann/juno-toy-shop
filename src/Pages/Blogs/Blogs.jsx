import React from 'react';
import useTitle from '../../CustomHook/useTitle';

const Blogs = () => {
    useTitle('Blogs')
    return (
        <div>
            <h1>blogs</h1>
        </div>
    );
};

export default Blogs;